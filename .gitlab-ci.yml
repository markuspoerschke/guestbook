before_script:
  - php -v
  - curl -sS https://getcomposer.org/installer | sudo php -- --install-dir=/usr/local/bin --filename=composer
  - composer install

test:5.6:
  image: php:5.6
  script:
    - composer test

test:latest:
  image: php
  script:
    - composer test
