before_script:
  - bash ci/gitlab/docker_install.sh
  - php -v
  - php bin/load-schema.php
  - composer install

services:
  - mysql

variables:
  MYSQL_ROOT_PASSWORD: root
  MYSQL_DATABASE: guestbook
  DB_DSN: mysql:dbname=guestbook;host=mysql
  DB_USER: root
  DB_PASSWORD: root

test:5.6:
  image: php:5.6
  script:
    - composer test

test:latest:
  image: php
  script:
    - composer test
